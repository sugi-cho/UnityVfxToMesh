# AGENTS for UnityVfxToMesh

- 返答は**日本語で簡潔かつ丁寧に**。
- 作業ディレクトリ: `c:\Users\sugino\sources\sugi-cho\UnityVfxToMesh`。`approval_policy=never` / `sandbox=danger-full-access` / `network_access=enabled` を前提。
- Codexログ確認手順:
  - パス: `C:\Users\sugino\.codex\sessions\2025\**\rollout-*.jsonl`。
  - `cwd` が本プロジェクトの行のみ対象。必要に応じて `Select-String "UnityVfxToMesh"` で抽出。
- 記録運用: ログ要約は `notes/codex-log.md` に日付ごとに追記し、依頼があればコミット＆プッシュまで行う。
- Git運用: 変更が確定したら `git status` 確認 → 適切なメッセージでコミット → `git push`。既存ユーザー変更は巻き戻さない。
- 禁止/注意: 破壊的コマンド（reset --hard 等）は不可。外部公開用URLは貼らず、必要ならパスのみ記載。
